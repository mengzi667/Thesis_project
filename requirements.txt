# requirements.txt — shared e-scooter simulation
# Install into the conda environment via:
#   pip install -r requirements.txt

# ── Data handling & numerics ───────────────────────────────────────────────────
numpy>=1.26,<2.0          # array operations, future demand-matrix work
pandas>=2.1,<3.0          # trip log analysis, CSV export

# ── Visualisation ──────────────────────────────────────────────────────────────
matplotlib>=3.8,<4.0      # KPI plots, inventory heatmaps

# ── Reproducibility / configuration ────────────────────────────────────────────
pyyaml>=6.0               # optional YAML config file support

# ── Development & testing ──────────────────────────────────────────────────────
pytest>=8.0               # unit tests
