# requirements.txt — thesis simulation workspace
# Install:
#   pip install -r requirements.txt

# ── Core runtime (current main.py project) ───────────────────────────────────
numpy>=1.26,<2.0
pandas>=2.1,<3.0
h3>=3.7,<4.0

# ── Optional (sara_repo execution) ────────────────────────────────────────────
# Needed if you run sara_repo/main.py and export Excel files.
xlsxwriter>=3.2

# Gurobi Python API is required only for sara_repo optimization runs.
# Install this only when Gurobi is available/licensed in your environment.
gurobipy>=11.0

# ── Optional development utilities ────────────────────────────────────────────
pytest>=8.0
